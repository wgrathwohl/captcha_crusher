You must install tensorflow to run this (https://www.tensorflow.org/versions/master/get_started/os_setup.html)

to run call python live_model.py filenames.txt outfile.txt where filenames.txt contains the names of the image files you'd like to get labels for and outfile.txt is where the results will be saved in json format